# Training Reports

Contains outputs from `model/training/train.py`.

## Files
- `training_curves.png` - Loss/accuracy plots over epochs
- `training_history.json` - Epoch-by-epoch metrics (loss, accuracy, learning rate)

## Note
Model weights (`cryingsense_cnn_best.pth`) are saved in `model/saved_models/`
