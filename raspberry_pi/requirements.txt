# CryingSense — Raspberry Pi 3B+ dependencies
# Install: pip install -r requirements.txt
#
# onnxruntime is the preferred inference backend on ARM.
# PyTorch is only needed if you use .pt / .pth models.

# ── Core audio ───────────────────────────────────────────────
numpy>=1.24,<2
librosa>=0.10,<0.11
soundfile>=0.12
PyAudio>=0.2.13

# ── Noise reduction (optional but recommended) ──────────────
noisereduce>=3.0

# ── Inference — pick ONE backend (ONNX recommended for Pi) ──
onnxruntime>=1.17              # ~60 MB, fast on ARM Cortex-A53

# ── Database ─────────────────────────────────────────────────
pymongo[srv]>=4.6

# ── Network / App communication ─────────────────────────────
websockets>=12.0
aiohttp>=3.9

# ── Config ───────────────────────────────────────────────────
python-dotenv>=1.0
